import React from "react";
import Layout from "../components/generic/layout";
import AvailableProducts from "../components/home/service providers/AvailableProducts";

const Home = () => {
    return (
        <Layout>
            <AvailableProducts />
        </Layout>
    );
};

export default Home;
